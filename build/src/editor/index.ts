/**
 * Website builder editor — headless GrapesJS + shadcn shell.
 */

export { Editor, type EditorProps } from "@/editor/components/Editor";
export { GrapesEditorProvider, useGrapesEditor } from "@/editor/context/EditorContext";
